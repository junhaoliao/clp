/**
 * Padding for the table to the bottom of the page.
 */
const TABLE_BOTTOM_PADDING = 95;

/**
 * The maximum number of results to retrieve for a search.
 */
const SEARCH_MAX_NUM_RESULTS = 1000;

/**
 * Number of search results to load per batch.
 */
const SEARCH_RESULTS_BATCH_SIZE = 10;

/**
 * Distance from the bottom of the scroll container (in pixels) at which to trigger loading more
 * results.
 */
const SCROLL_LOAD_MORE_THRESHOLD_PX = 200;

export {
    SCROLL_LOAD_MORE_THRESHOLD_PX,
    SEARCH_MAX_NUM_RESULTS,
    SEARCH_RESULTS_BATCH_SIZE,
    TABLE_BOTTOM_PADDING,
};
